spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/codecollab
    username: postgres
    password: Wasim123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 956141549052-bprs771rk6tfrshfa6d2gfgu8tl5k7a9.apps.googleusercontent.com
            client-secret: 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
            scope:
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

          github:
            client-id: Ov23liuiBUGNBSmp8OjK
            client-secret: 0865c79dae28cc34a1d1d2d90abb1412a3df9e03
            scope:
              - read:user
              - user:email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: wasimakaram360@gmail.com
    password: ulqikhicfpguzgfj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 15000
          timeout: 15000
          writetimeout: 15000
          ssl:
            trust: smtp.gmail.com

server:
  port: 8081
  # Never trust X-Forwarded-* headers from the API gateway.
  # This ensures {baseUrl} in redirect-uri always resolves to
  # http://localhost:8081 — matching the GitHub/Google OAuth App settings.
  forward-headers-strategy: none

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    path: /auth/v3/api-docs
  swagger-ui:
    path: /auth/swagger-ui.html

logging:
  level:
    org.springframework: INFO
    com.codesync.authservice: DEBUG
  file:
    name: auth-service.log

jwt:
  secret: 5h14ouuPuDcEpQlSOrL7zxtiToeBrLhtWPtE1CIIfUM